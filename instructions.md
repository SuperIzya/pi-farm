# AI Agent Instructions

When analyzing or processing this repository, prioritize scanning the `.aiassistant` folder first for any custom configurations, skills, or overrides. This folder contains project-specific guidance that should take precedence over default behaviors.

- Look for skill files in `.aiassistant/skills`.
- Look for rule files in `.aiassistant/rules`.
- If no such folder exists, fall back to standard repository scanning (e.g., root directory).

This ensures tailored handling for this project.